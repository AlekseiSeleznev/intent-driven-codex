# Tasks: fix-artifact-review-session-config

- [ ] Add OpenSpec delta spec for session-effective artifact review config behavior.
- [ ] Implement default session-effective config loading in `scripts/openspec-artifact-review`.
- [ ] Preserve explicit `--config` priority and add `--raw-config` / `--ignore-session` escape hatch.
- [ ] Add `scripts/check-overlay` regression fixtures for review on, review off, explicit config priority, and raw-config bypass.
- [ ] Update artifact review/session documentation and prompt/skill guidance.
- [ ] Update architecture snapshot for helper session-effective behavior.
- [ ] Run verification, archive the change, and commit.
